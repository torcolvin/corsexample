This is an example webpage created with gemini to make basic fetch requests.

Edit connect.js CONIFG to specify the endpoint and credentials

To run the example:

$ npx serve

Then open <http://localhost:3000> in your browser. The origin will be <http://localhost:3000>
